# Аудит проекта «Думающая система»

*Независимый аудит от 2026-07-02. Методика: 17 проверяющих агентов (8 аудиторов по подсистемам,
8 адверсариальных верификаторов, критик полноты), ~30 из 57 run-скриптов реально запущены,
все ключевые числа README и коммитов сверены с фактическим выводом, код прочитан на предмет
утечек held-out, захардкоженных ответов и подгонки сидов.*

## Общий вердикт

Это **настоящий, добротно сделанный исследовательский прототип** — не обёртка над LLM.
Все механизмы реализованы руками на numpy (backprop/Adam, JEPA-подобная модель мира,
байесовский фильтр, Q-опции, DreamCoder-lite, GEM-метрики), всё детерминированно
воспроизводится, захардкоженных ответов и подгонки сидов **не найдено нигде**.
165 тестов (163 passed, 2 skipped; 1 тест падает без pymupdf — нужен `importorskip`).

Главные проблемы — не в коде, а в **риторике**: заголовки README и коммитов регулярно
сильнее показанного, а ключевые заявления про реальные данные (ARC, книги) невоспроизводимы
из репозитория. Уровень зрелости: связная серия честных **игрушечных демонстраций**
когнитивной архитектуры, а не бенчмарки.

## Что подтвердилось (воспроизведено запуском, выдержало адверсариальную проверку)

| Заявление | Факт |
|---|---|
| MVP: ошибка падает ~99%, бьёт persistence | 98.9%; бьёт baseline на 24 сидах (×2.6–9.0, медиана ×3.8). Оговорка: «99%» меряется от случайной инициализации — persistence от той же точки дал бы 97.5%; содержательная цифра — отрыв от baseline |
| Шаг 3: replay обнуляет забывание | Бит-в-бит (naive 0.38 / replay −0.0007); compute равный (3932 vs 3935 апдейтов — проверено инструментацией); держится на честно отложенном сегменте и сидах 1–3 |
| Книги: модель бьёт unigram на held-out | 2.42 vs 5.42 бит/байт; утечки нет (срез без шаффла, unigram только по train); при честном разделении val/test — 2.09 vs 5.50 |
| Единый контур (run_agent) | 5.28→2.20 бит/байт; аблация консолидации: 0.11–0.25 vs 3.55–4.00 BPC (×14–36); опечатка «retunr» найдена по surprise |
| Мир: 794→24 шага; POMDP 100% vs 57%; unified 8/8 | Всё воспроизведено; устойчиво на 10/5/3 сидах. Попытки опровержения **усилили** результат: безпамятный бейзлайн на 10 сидах — 33%, а более сильный реактивный бейзлайн со знанием карты — лишь 39% vs 100% у belief-агента |
| Перенос навыка (run_transfer) | Zero-shot 60% vs 5% на 20 **свежих случайных лабиринтах** — единственное в проекте настоящее свидетельство обобщения на distribution shift |
| Рост DSL-библиотеки | Механизм работает: абстракции сокращают поиск 194→24 проверенных программ на held-out входах; поиск честный (вне языка → None); run_benchmark 10/10 с показом провалов |
| Причинность (run_causal) | Корректно различает «видеть»/«делать», контрфактика работает; граф задан вручную (признано в самом скрипте) |

## Что опровергнуто или сильно завышено

1. **«Честный замер на ARC» (коммит e4d24ad) — опровергнуто как сформулировано.**
   В репозитории нет ни датасета ARC, ни загрузчика, ни логов прогона. Цифры из тела коммита
   («400 train, 22 решения, 8 многошаговых», задача 8f2ea7aa, «~5%») невоспроизводимы.
   Все «ARC»-задачи в скриптах — 4–8 ручных сеток, цели которых построены композициями
   **тех же примитивов**, которыми решаются, — решаемость гарантирована по построению.
   Протокола ARC (train-пары + скрытый тест-вход) нет нигде. Рост библиотеки даёт выигрыш
   в эффективности поиска, но **+0 новых задач** (тело коммита это честно признаёт —
   нечестен именно заголовок).

2. **«100% понимание невиданных формулировок» — опровергнуто как «понимание».**
   Held-out — случайные 30% декартова произведения (30 слов × 5 классов); 6 из 144
   тест-команд буквально есть в train, 69% отличаются только артиклем «the»; тривиальный
   keyword-бейзлайн из 10 строк тоже даёт 100%. Классификатор слеп к отрицанию
   («do not go to the top left» → идёт в top-left, conf 0.94) и порядку слов; композиция
   известных слов в неудачном порядке роняет систему необработанным TypeError.

3. **«Любопытство избегает шум: ~5% против ~25%» — черри-пик.**
   Цифра 5.4% — только средняя фаза seed 0. На 10 сидах за весь прогон: 23.2% vs 25.1%
   у random (слабый эффект); на 3 из 10 сидов любопытство тратит на шум **больше** random;
   ошибка хуже random на 4/10 сидов; куррикулум easy→medium→hard виден на ~2/10 сидов.
   Пороги (lp_z, lp_floor) — несущие и подогнаны под масштаб именно этой среды.
   Устойчиво лишь одно (и это правда во всех 10 сидах): максимум внимания — на выучиваемом
   канале, и перманентной фиксации на шуме нет.

4. **README устарел в ~4 раза**: «34/41 тест» при фактических 165; ~36 коммитов из 50
   (reasoning/, language/, RL-опции, перенос, lifelong) в README вообще не описаны;
   табличные числа по книгам (4.25/4.91) непроверяемы — книг в репо нет (.gitignore).

## Системные слабости (общие для всех подсистем)

- **Нет held-out задач** — почти всюду отложены только новые *входы* того же преобразования,
  а не новые задачи/миры/формулировки. Исключение — run_transfer (новые лабиринты).
- **Циркулярность постановок**: задачи конструируются из тех же примитивов/шаблонов,
  которыми решаются (reasoning, language, benchmark).
- **run_step3**: буфер 50k вмещает весь опыт → «replay» фактически полный rehearsal
  (верхняя граница), а не ограниченная консолидация.
- **Инфраструктура**: нет CI/линтеров/smoke-тестов 54 корневых скриптов и единой точки
  входа; 6 дублирующихся grid-сред без общего интерфейса; бог-модуль `__init__.py`
  (268 строк); DSL-библиотека не сериализуется (рост живёт в памяти одного процесса;
  `grow_from_solutions` падает StopIteration на чужом примитиве).
- **Провенанс**: история ядра пришла одним bulk-коммитом на 60 файлов; репозиторий —
  переиспользованный форк obsidian-releases, имя не соответствует содержимому.

## Что реально получается (сильные стороны)

- Связная исследовательская программа с осмысленной дугой: предсказание → любопытство →
  консолидация → реальные тексты → мир/POMDP/JEPA → язык → иерархия → RL-опции → перенос →
  рассуждение → рост DSL. Каждый шаг = механизм + тесты + демо.
- Честная методика **по конструкции** в ядре: test-then-train, позиционные сплиты,
  бейзлайны считаются реально, негативные контроли есть, система честно отвечает
  None/«не понял», провалы печатаются.
- Документация выше любительского уровня: каждый модуль объясняет «почему» со ссылками
  (JEPA, DreamCoder, CLS, GEM, Oudeyer); research-док — добротный обзор; план MVP из него
  реализован полностью и в заявленном порядке.
- Автор сам проговаривает ограничения в докстрингах — завышение живёт в основном
  в заголовках и README.

## Рекомендации (по убыванию ценности) — статус выполнения см. ниже

1. **Сделать замер на ARC реальным**: добавить загрузчик ARC-AGI (данные публичны, JSON)
   и скрипт замера с протоколом train-пары → скрытый тест-вход. Это превратит самое
   слабое заявление проекта в самый сильный результат — механизм роста библиотеки уже готов.
2. **Ввести held-out задач везде**: новые миры (расширить подход run_transfer), новые
   слова/синонимы вне train-словаря в языке, новые типы преобразований в reasoning.
3. **Починить статистику любопытства**: репортить все фазы × много сидов (среднее ± разброс)
   вместо средней фазы seed 0; перекалибровать или убрать заявление «избегает шум».
4. **Актуализировать README** и пометить все внешние прогоны («не в репо») явно.
5. **Инфраструктура**: CI с pytest + smoke-тесты скриптов; `pytest.importorskip("fitz")`;
   ограничить буфер в run_step3 (или репортить обе конфигурации); сериализация DSL-библиотеки;
   консолидировать grid-среды; перенести проект в свой репозиторий с говорящим именем.

## Дополнение: рекомендации выполнены (2026-07-03)

1. ✅ **ARC реальный**: `reasoning/arc_data.py` + `run_arc.py` (протокол train-пары →
   скрытый test; данные через `pip install -e '.[arc]'`). Реальные числа: training
   **22/400 (5.5 %)** — прежние заявления коммита подтвердились; evaluation
   **4/400 → 6/400** после роста библиотеки из training-решений — первый настоящий
   перенос роста языка между непересекающимися наборами задач.
2. ✅ **Held-out задач**: язык — сплит по фразам (`split_by_phrase`): рекомбинации 100 %,
   невиданные фразы **30 %** (честное число вместо «100 % понимания»); миры — run_world
   меряет 10 свежих лабиринтов (10/10, ×1.02 от оптимума); reasoning — ARC-eval сплит.
3. ✅ **Статистика любопытства**: run_step2 агрегирует по сидам (среднее ± σ, счётчики);
   черри-пик и безусловное «УСТОЙЧИВО» убраны; неустойчивость куррикулума и слабость
   избегания шума печатаются явно.
4. ✅ **README актуализирован**: 172 теста, внешние прогоны помечены «вне репо»,
   реальные числа ARC/любопытства/консолидации, описаны RL- и reasoning-подсистемы.
5. ✅ **Инфраструктура**: CI (pytest + 7 smoke-демо), `importorskip("fitz")`,
   run_step3 печатает полный и ограниченный (500 переходов) буфер рядом
   (naive 0.38 / full −0.00 / limited 0.30 — «обнуление» только у полного rehearsal),
   `LibraryLearner.save/load` + устойчивость к чужим примитивам.
   Консолидация grid-сред выполнена отдельным шагом: логика шага/стен/границ и
   списков свободных клеток теперь живёт ТОЛЬКО в `GridWorld` (in_bounds / is_free /
   move_from / move_sid / free_cells / free_sids); удалено ~40 копий в 30 файлах
   (все 6 сред, 10 методов `_move` агентов, обёртки, корневые скрипты); поведение
   проверено бит-в-бит diff-ом выводов 21 демо до/после + 170 тестов.
   Отложено: перенос в отдельный репозиторий (действие владельца, не кода).

## Дополнение 2: интегративный шаг «единый агент» (2026-07-03)

Закрыт главный концептуальный разрыв «думают компоненты — не думает система»
на уровне первого интегрированного прототипа (`thinking_system/mind/`,
`run_mind.py`): один постоянный агент сам маршрутизирует опыт (текст/сетки),
заземляет слова учебника в грид-примитивы индукцией из показов, растит из
определений библиотеку, эскалирует бюджет размышления с трудностью, хранит
состояние между запусками и по собственной повестке возвращается к нерешённому.
Замер честный, на скрытых test-входах реального ARC evaluation: 4 → 6 задач
после ЧТЕНИЯ учебника; стоимость поиска по мере жизни агента: 645 → 145 → 26
проверенных программ. Открытыми остаются: открытый словарь, самостоятельный
выбор материала для чтения.

## Дополнение 3: переменные в языке (2026-07-03)

Два вида переменных, оба в жизни агента и в честном замере:
(1) параметрические примитивы `keep[c]`/`drop[c]`/`paint[c]`
(`reasoning/parametric.py`) — аргумент-цвет связывается из палитры конкретной
задачи; во всех ступенях поиска Mind и в `run_arc.py --parametric`. Замер:
training 22 → 23 (5.8 %, добавилась a740d043 «drop[1] ▸ bbox»), evaluation
без изменений (4) — честно отражено.
(2) шаблоны с дыркой (`reasoning/templates.py`) — анти-унификация накопленных
решений («keep[3] ▸ bbox» + «keep[2] ▸ bbox» → «? ▸ bbox»), заполнение дырки
стоит |P| программ вместо |P|^len; ступень после лестницы размышления.
Честный итог: при нынешнем опыте (6 решений, почти все одношаговые) шаблоны
новых задач ещё не дают — им нужны разнообразные глубокие решения; механизм
покрыт тестами (в т.ч. глубина 3 по цене одного слота).

## Дополнение 4: объект как переменная (2026-07-03)

Третий вид переменных (`reasoning/object_param.py`): комбинатор `each[f]`
применяет f к вырезке каждого связного объекта (та же 4-связность, что у
keep_largest) и собирает сетку обратно; `pick[k]` оставляет k-й по размеру
объект. В each-семействе только операции, где пообъектное применение отличается
от глобального (симметрии, gravity); f, меняющая размер вырезки, — честно
недопустимый шаг. Замер (`run_arc.py --parametric --objects`, скрытые тесты):
evaluation 4 → 5 базовым замером — задача 8ee62060 «flip_h ▸ each[flip_h]»;
вместе с ростом библиотеки evaluation 7 (1.8 %). Жизнь Mind целиком:
5 (переменные) → 7 (после чтения учебника); стоимость размышления по мере
жизни 1378 → 260 → 26 программ. Итоговая траектория проекта на eval-сплите:
4 → 5 → 7 при неизменно честном протоколе.

"""Интерфейс политики выбора действия.

Политика получает по каждому действию его эпистемическую ценность (ожидаемый
прирост информации) и прагматическую ценность (приближение к целям) и выбирает
действие. В терминах активного вывода — минимизирует ожидаемую свободную энергию
G = −(эпистемическая + прагматическая ценность).
"""

from __future__ import annotations

from abc import ABC, abstractmethod

import numpy as np


class Policy(ABC):
    """Абстрактная политика выбора действия по ценностям."""

    @abstractmethod
    def select_action(self, epistemic: np.ndarray, pragmatic: np.ndarray | None = None) -> int:
        """Выбрать индекс действия.

        Args:
            epistemic: ожидаемый прирост информации по каждому действию, форма (n_actions,).
            pragmatic: ценность приближения к целям (или None, если целей нет).
        """
